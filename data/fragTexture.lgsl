#version 410 core

out vec4 FragColor;
uniform vec4 ourColor;

uniform sampler2D ourTexture;
in vec2 TexCoord;

void main()
{
	vec4 myCol = vec4(1.0f,1.0f,0.0f,1.0f);
   FragColor = mix(myCol,texture(ourTexture, TexCoord),1.0);
}